import type { Agent, AgentState, ThinkingLevel } from "@mariozechner/pi-agent-core";
import type { Model } from "@mariozechner/pi-ai";
import { type SessionManager } from "../session-manager.js";
import type { SettingsManager } from "../settings-manager.js";
/**
 * TUI renderer for the coding agent
 */
export declare class TuiRenderer {
    private ui;
    private chatContainer;
    private pendingMessagesContainer;
    private statusContainer;
    private editor;
    private editorContainer;
    private footer;
    private agent;
    private sessionManager;
    private settingsManager;
    private version;
    private isInitialized;
    private onInputCallback?;
    private loadingAnimation;
    private lastSigintTime;
    private lastEscapeTime;
    private changelogMarkdown;
    private queuedMessages;
    private streamingComponent;
    private pendingTools;
    private thinkingSelector;
    private queueModeSelector;
    private themeSelector;
    private modelSelector;
    private userMessageSelector;
    private sessionSelector;
    private oauthSelector;
    private isFirstUserMessage;
    private scopedModels;
    private toolOutputExpanded;
    private hideThinkingBlock;
    private unsubscribe?;
    private fileCommands;
    constructor(agent: Agent, sessionManager: SessionManager, settingsManager: SettingsManager, version: string, changelogMarkdown?: string | null, scopedModels?: Array<{
        model: Model<any>;
        thinkingLevel: ThinkingLevel;
    }>, fdPath?: string | null);
    init(): Promise<void>;
    private subscribeToAgent;
    private checkAutoCompaction;
    private handleEvent;
    private addMessageToChat;
    renderInitialMessages(state: AgentState): void;
    getUserInput(): Promise<string>;
    private rebuildChatFromMessages;
    private handleCtrlC;
    private updateEditorBorderColor;
    private cycleThinkingLevel;
    private cycleModel;
    private toggleToolOutputExpansion;
    private toggleThinkingBlockVisibility;
    clearEditor(): void;
    showError(errorMessage: string): void;
    showWarning(warningMessage: string): void;
    showNewVersionNotification(newVersion: string): void;
    private showThinkingSelector;
    private hideThinkingSelector;
    private showQueueModeSelector;
    private hideQueueModeSelector;
    private showThemeSelector;
    private hideThemeSelector;
    private showModelSelector;
    private hideModelSelector;
    private showUserMessageSelector;
    private hideUserMessageSelector;
    private showSessionSelector;
    private handleResumeSession;
    private hideSessionSelector;
    private showOAuthSelector;
    private hideOAuthSelector;
    private handleExportCommand;
    private handleCopyCommand;
    private handleSessionCommand;
    private handleChangelogCommand;
    private handleClearCommand;
    private handleDebugCommand;
    private compactionAbortController;
    private executeCompaction;
    private handleCompactCommand;
    private handleAutocompactCommand;
    private updatePendingMessagesDisplay;
    stop(): void;
}
//# sourceMappingURL=tui-renderer.d.ts.map