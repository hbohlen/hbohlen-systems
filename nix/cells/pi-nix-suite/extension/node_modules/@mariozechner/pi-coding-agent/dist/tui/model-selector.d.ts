import type { Model } from "@mariozechner/pi-ai";
import { Container, Input, type TUI } from "@mariozechner/pi-tui";
import type { SettingsManager } from "../settings-manager.js";
/**
 * Component that renders a model selector with search
 */
export declare class ModelSelectorComponent extends Container {
    private searchInput;
    private listContainer;
    private allModels;
    private filteredModels;
    private selectedIndex;
    private currentModel;
    private settingsManager;
    private onSelectCallback;
    private onCancelCallback;
    private errorMessage;
    private tui;
    constructor(tui: TUI, currentModel: Model<any> | null, settingsManager: SettingsManager, onSelect: (model: Model<any>) => void, onCancel: () => void);
    private loadModels;
    private filterModels;
    private updateList;
    handleInput(keyData: string): void;
    private handleSelect;
    getSearchInput(): Input;
}
//# sourceMappingURL=model-selector.d.ts.map